---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

includes:
  dev:
    taskfile: ./.tasks/development.yml
  agent-log:
    taskfile: ./.tasks/business/agent-log.yml
  benchmark:
    taskfile: ./.tasks/business/benchmark.yml

tasks:
  default:
    silent: true
    cmds:
      - task --list
